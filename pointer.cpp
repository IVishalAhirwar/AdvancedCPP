#include<stdio.h>
#include<iostream>
int main()
{


    // int*pint=nullptr;//protect pointer to access random memory location;
    // int x=std::cin.get();
    // int* ptr=0;
    // pint=&x;//adress
    // *pint=450;//value

    // std::cout<<"Address of pint pointer>>"<<ptr<<"Value of X >>"<<&x;

    // int size;
    // puts("Enter Size of Memery To Allocate>>");
    // scanf("%d",&size);
    // int* memory=new int[size];
    // for(int i=0;i<size;i++)
    // {
    //     std::cin>>memory[i];
    // };
    // for(int i=0;i<size;i++)
    // {
    //     std::cout<<memory[i];
    // };

    // delete [] memory;
    // for(int *data = memory;*data!=0;data++)
    // {
    //     printf(">>%d\n",memory[5]);
    // };
    // std::cout<<"0x"<<_HEX<<memory;
    // int* ptr;
    // ptr =new int[5];
    // ptr[5]=NULL;

    // // for(int*nptr=ptr;nptr!=nullptr;++nptr)std::cout<<*nptr;
    // int x;
    // int*const ptr=&x;//value can be changed but address Can't be Changed

//     int const*cptr;//Value can't be changed but adress can be changed

//     const int* const ccptr=&x;
//  int const*cccptr;
//     int z;
//     *cccptr=46;
//     cccptr=&z;
//     //*ccptr=56;
//     int y;
//     ccptr=&y;
//     cptr=&x;
//     //*cptr=56;
//     *ptr=456;
//     //ptr =34s

// int arr[3];
// int* ptr=arr;
// for(int i=0;i<3;i++)
// {
//     std::cin>>ptr[i];
// };
// for(int i=0;i<3;i++)
// {
//     std::cout<<"\nptr>>"<<&ptr[i];
// };
// for(int i=0;i<3;i++)
// {
//     std::cout<<"\nArr>>"<<arr[i];
// };
// bool*IsSunny;
// puts("Is Sunny [y/n]>>");
// char c;
// scanf("%c",&c);
// if(c=='y'||c=='Y')
// {
//     IsSunny=new bool;
//     *IsSunny=true;
// }else
// {
//     if(IsSunny!=nullptr)
//     {
//         puts("Detting...");
//         delete IsSunny;
//         IsSunny=nullptr;

//     }
//      if(IsSunny==nullptr)
//      {
//          puts("IsSunny is NULL");
//       IsSunny=new bool;
//      *IsSunny=false;
// };

// };
// std::cout<<"IsSunny Flags Address >> "<<IsSunny;
// std::cout<<"\nIsSunny Flags Value >> "<<*IsSunny;
// std::cin.clear();
// std::cin.ignore();
// if(IsSunny!=nullptr)
// {
//     puts("Deleting IsSunny...");
// delete IsSunny;
// };

//try
//{
    // int*ptr=new int[0x1fffffff];
    // delete []ptr;
//}
// catch(std::bad_alloc&e)
// {
//     std::cerr << e.what() << '\n';
// };




    getchar();

    return 0;

};
